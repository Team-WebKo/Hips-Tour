file:
  upload-dir: uploads/

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: abc123
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true